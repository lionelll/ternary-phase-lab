# 三元相图项目开发规范

本文件是本仓库的强制执行规则。任何代码修改、Git 操作和部署操作开始前，都必须先阅读并遵守本文件。

## 1. 分支规则

- 所有功能开发、缺陷修复、文案修改、样式调整、测试修改和配置修改必须在 `dev` 分支进行。
- 开始修改前必须执行分支检查，确认当前分支为 `dev`。
- `release` 分支禁止直接开发、直接修改、直接提交或补写代码。
- `release` 只能合并已经在 `dev` 完成并验证通过的代码。
- 如果发现未提交修改误落在 `release`，必须停止继续开发，先将全部修改安全迁移到 `dev`，并确认 `release` 没有产生新提交。

## 2. 提交、推送与部署授权

- 本地修改完成后，默认停留在未提交状态等待用户确认。
- 只有用户明确要求“提交”时，才可以创建 Git 提交。
- 只有用户明确要求“推送/同步到 GitHub”时，才可以推送远程分支。
- 只有用户明确要求“部署到测试服务器”时，才可以部署测试环境。
- 只有用户明确要求“合并到 release”时，才可以把 `dev` 合并到 `release`。
- 只有用户明确要求“部署正式环境”时，才可以操作正式服务器。
- 不得因为代码已经完成而自行提交、推送、合并、打标签或部署。

## 3. 标准发布顺序

1. 在 `dev` 完成功能开发与本地验证。
2. 经用户授权后提交并推送 `dev`。
3. `dev` 自动部署测试环境，等待部署任务成功并核验测试地址。
4. 经用户明确授权后，将 `dev` 合并到 `release`；禁止在 `release` 补改代码。
5. 经用户明确授权后部署正式环境，并核验域名、HTTPS 和 `/release.json` 提交号。
6. 正式部署成功后，必须按语义化版本顺序创建并推送下一个正式标签，例如 `v1.0.0`、`v1.0.1`、`v1.0.2`；不得跳号、复用或遗漏。
7. 正式标签必须准确指向本次已经部署并完成核验的 `release` 提交；禁止把标签打在 `dev`、未部署提交或其他分支提交上。
8. 标签推送成功后，更新正式环境版本记录。正式部署只有在“服务器核验成功＋标签核对成功”后才算完整结束。

### 正式发布完成检查（强制）

- 正式环境只能使用 `npm run build:production` 构建；该命令必须把 Vite 基路径设为 `/`。测试环境继续使用 `/ternary/`，两者禁止混用。
- 发布前检查 `static-dist/index.html`：正式包内 JS、CSS、Logo 地址必须以根路径 `/` 开头，不得出现 `/ternary/assets/` 或 `/ternary/brand-logo.png`。
- 正式域名可通过 HTTPS 访问，HTTP 正确跳转到 HTTPS。
- `/release.json` 的 `commit` 与本次部署的 `release` 完整提交号一致。
- 新标签按现有最高版本顺序递增，并且准确指向上述提交。
- 新标签已经推送到 GitHub；仅创建本地标签不视为发布完成。
- 完成汇报必须同时列出正式地址、部署提交、正式标签和核验结果。

## 4. 修改范围

- 只允许修改三元相图仓库：`ternary-phase-lab`。
- 晶体结构项目仅可作为只读视觉参考，禁止修改其任何代码或资源。
- 不得修改与用户需求无关的几何、交互、布局、文案或部署配置。
- 修改三维模型时必须保持渲染几何、相区判定和教学交互使用一致的数据来源。

## 5. 验证要求

- 提交前至少执行：
  - TypeScript 类型检查；
  - ESLint；
  - 相图几何测试；
  - 静态生产构建。
- 三维视觉或交互修改必须进行本地浏览器核验，覆盖三个模型及受影响功能。
- 不得放宽已有几何、拓扑和共享边界测试来掩盖实现错误。
- 发现既有警告时应如实记录，不得把警告描述为错误，也不得隐瞒新增错误。

## 6. 安全与凭据

- 服务器密码、GitHub Token、私钥和其他敏感凭据禁止写入 Git、源码、脚本、README、日志或普通说明文件。
- SSH 操作必须校验服务器主机指纹，并使用明确的服务器地址和部署目录。
- 正式环境必须使用不可变版本目录和原子切换方式发布，保留可回滚版本。
- 禁止执行可能破坏仓库、服务器根目录或用户文件的宽范围删除命令。

## 7. 项目环境资料

- 测试与正式环境的非敏感信息记录在仓库同级文件：`../三元相图项目部署环境说明.md`。
- 测试分支：`dev`。
- 正式分支：`release`。
- 正式域名：`https://ternary.changyanedu.cn/`。
